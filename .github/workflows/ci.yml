name: CI

on:
  push:
    paths:
      - grammar.json
      - src/**
      - queries/**
      - web/**
      - .github/**
  pull_request:
    branches: [main]
    paths:
      - grammar.json
      - src/**
      - queries/**
      - web/**
      - .github/**
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Tree-sitter
        uses: ./.github/actions/setup

      - run: tree-sitter test

  soundness:
    name: Fuzz Soundness
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Tree-sitter
        uses: ./.github/actions/setup

      - name: Fetch Fuzz Testing Corpus
        shell: bash
        run: ./test/fuzz/fetch-corpus.sh

      - name: Run Soundness Tests
        shell: pwsh
        run: .\test\fuzz\soundness.ps1

      - uses: actions/upload-artifact@v7
        with:
          name: Soundness Fuzz Test Report
          path: ./test/fuzz/findings/

  fuzz-parser:
    name: Fuzz Parser
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Fetch Fuzz Testing Corpus
        shell: bash
        run: ./test/fuzz/fetch-corpus.sh

      - name: Fuzz Parser
        uses: tree-sitter/fuzz-action@v4
        with:
          corpus: ./test/fuzz/corpus

  compile:
    name: Compile and Upload Artifacts
    runs-on: windows-latest
    needs: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Tree-sitter
        uses: ./.github/actions/setup

      - run: tree-sitter build -o ./tree-sitter-autohotkey.dll

      - uses: actions/upload-artifact@v7
        with:
          name: Compiled Binary & Build Artifacts
          path: |
            ./tree-sitter-autohotkey.dll
            ./src/

  publish:
    name: Publish to crates.io
    needs: test
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    uses: ./.github/workflows/publish.yml
    secrets: inherit

  deploy-pages:
    name: Deploy Playground to Pages
    needs: test
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/deploy-pages.yml
